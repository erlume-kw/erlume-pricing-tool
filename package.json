{
  "name": "erlume-pricing-tool",
  "version": "1.0.0",
  "description": "erlume internal AI bag pricing tool — UI only; the Pricing 3.0 logic lives in the erlume backend under /api/pricing-tool",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "echo \"Formula tests live in the backend: npx ts-node src/scripts/test-pricing-formula.ts\" && exit 1"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
